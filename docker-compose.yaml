version: '3.8'

services:
  app-backend:
    build: .
    container_name: projekt-backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://user:password@db:5432/database
    depends_on:
      db:
        condition: service_healthy

  db:
    image: postgres:15-alpine
    container_name: projekt-db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: database
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d database"]
      interval: 5s
      timeout: 5s
      retries: 5


  prometheus:
    image: prom/prometheus
    container_name: projekt-prometheus
    volumes:
      - ./prometheus.yaml:/etc/prometheus/prometheus.yml
      - ./alert_rules.yml:/etc/prometheus/alert_rules.yml
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana
    container_name: projekt-grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    depends_on:
      - prometheus
  adminer:
    image: adminer
    container_name: projekt-adminer
    ports:
      - "8081:8080"
    depends_on:
      - db

  app-frontend:
    build:
      context: ./library-canvas-main
      dockerfile: Dockerfile
    container_name: projekt-frontend
    ports:
      - "80:8080"
    volumes:
      - ./library-canvas-main:/app
      - /app/node_modules

    depends_on:
      - app-backend

volumes:
  postgres_data:


